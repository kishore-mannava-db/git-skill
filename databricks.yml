bundle:
  name: git-skill

sync:
  exclude:
    - ".github/**"
    - ".assistant/**"
    - ".claude/**"
    - ".databricks/**"
    - "deploy_skills.sh"
    - "README.md"
    - ".gitignore"
    - "databricks.yml"

targets:
  dev:
    workspace:
      host: https://adb-2018611342634356.16.azuredatabricks.net
      file_path: /Workspace/Users/${workspace.current_user.userName}/.assistant/skills
    default: true
